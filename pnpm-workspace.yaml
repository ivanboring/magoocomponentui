packages:
  - packages/*
  - preview

# esbuild needs its platform binary (used by Astro/Vite). sharp (optional Astro
# image optimization) is not used by the preview, so its native build is skipped.
allowBuilds:
  esbuild: true
  sharp: false

onlyBuiltDependencies:
  - esbuild
