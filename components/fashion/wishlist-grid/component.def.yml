name: wishlist-grid
props:
  gap:
    type: enum
    values: [default, none, sm, md, lg, xl]
    default: default
    title: Gap
    description: >
      Space between grid cells, from a fixed set of choices. "default" keeps the component's
      designed spacing; none/sm/md/lg/xl are explicit fixed overrides.
  title:
    type: string
    default: "Your wishlist"
    title: Title
  count:
    type: string
    title: Count
    example: "6 items"
slots:
  items:
    title: Items
    description: The saved product tiles.
variants:
  gap:
    default: "gap-3"
    none: "gap-0"
    sm: "gap-3"
    md: "gap-6"
    lg: "gap-10"
    xl: "gap-16"
