name: goal-progress
props:
  goals:
    type: array
    items: object
    required: true
    title: Goals
    description: "Each goal: { label, current, target, pct, tone } where pct is 0-100."
    example:
      - { label: "New customers", current: "180", target: "250", pct: 72, tone: "primary" }
