name: widget-card
props:
  title:
    type: string
    required: true
    title: Title
    example: "Recent activity"
  subtitle:
    type: string
    title: Subtitle
    example: "Last 7 days"
slots:
  actions:
    title: Actions
    description: Trailing header actions (menu, filter).
  body:
    title: Body
    description: The widget content.
