name: report-filter-bar
props:
  dimensions:
    type: array
    items: object
    required: true
    title: Dimension filters
    description: "Each: { label, value }."
  measures:
    type: array
    items: object
    title: Measure chips
    description: "Each: { label, active }."
  active_filters:
    type: array
    items: object
    title: Active filter chips
    description: "Each: { label }."
