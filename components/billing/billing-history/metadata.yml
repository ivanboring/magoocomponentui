name: "Billing History"
short_description: An invoices list container with an export-all action.
long_visual_description: >
  A titled panel with an export-all action, containing a divided list of invoice rows.

use_cases:
  - Billing history list.
  - Invoices panel.
  - Payment history.
  - Statements list.
  - Receipts list.

recommended_for:
  - A container for invoice rows with export.
avoid_for:
  - A single invoice (use invoice-detail).

markets:
  - Global

example_usage: >
  Slot invoice-row elements; the export-all downloads everything.

props:
  title: The section title.

slots:
  items: The invoice rows.

relationships:
  children:
    - billing/invoice-row

lifecycle: experimental
content_model: A list of invoice rows with export.

theming:
  tokens_used:
    - --color-surface
    - --color-surface-raised
    - --color-on-surface
    - --color-border
    - --radius-card
    - --radius-button
    - --shadow-card
    - --font-heading
    - --space-card
    - --duration-token

editorial_guidance: >
  Order newest-first; offer export-all for records.

categorization:
  category: Billing
  subcategory: Invoices
  atomic_type: organism
  usage_type: [list-item]
  maturity: ai-generated
  wcag:
    level: AA
    notes: Uses a real section and heading; export is a real button; slotted rows carry their own semantics.
  keyboard_support: The export button is focusable and activates on Enter/Space.
  seo_score: 35
  text_direction: both
