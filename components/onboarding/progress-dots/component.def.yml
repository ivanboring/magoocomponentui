name: progress-dots
props:
  dots:
    type: array
    items: object
    required: true
    title: Dots
    description: "Each: { active }."
    example:
      - { active: false }
      - { active: true }
  label:
    type: string
    title: Label
    example: "Step 3 of 5"
